# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-openai-api-key-here

# DynamoDB Table Name (only for local development with DynamoDB)
# In AWS Lambda, this is automatically set by serverless.yml
CLAIMS_TABLE=claims-risk-assessor-claims-dev

# Server Port (for local development)
PORT=3000

# AWS Region (optional, defaults to us-east-1)
AWS_REGION=us-east-1
